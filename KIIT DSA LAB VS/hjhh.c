# include <stdio.h>
int main()
{

    printf(" \n enter the marks of the student");
    int a;
    scanf("%d",&a);
    if (a<40)
        printf(" the grade :  F");
     else;
     if (a>=40)
        printf(" the grade :  D");
     else;
    if (a>=50)
        printf(" the grade :  c");
    else;
    if (a>=60)
        printf(" the grade :  B");
    else;
    if (a>=70)
        printf(" the grade :  C");
    else;
    if (a>=80)
        printf(" the grade :  A");
      else;
    if (a>=90)
        printf(" the grade :  O");
    return 0;

}
